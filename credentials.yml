# Configuration for Rasa channels

rest:
  # REST channel - no configuration needed

# Add other channels as needed:
# slack:
#   slack_token: "your-bot-token"
#   slack_channel: "your-channel"
#   slack_signing_secret: "your-signing-secret"