# Dockerfile for Rasa server
FROM rasa/rasa:3.7.10-full

# Copy project files
COPY . /app
WORKDIR /app

# Set user
USER 1001

# Expose port
EXPOSE 5005

# Run Rasa server
CMD ["run", "--enable-api", "--cors", "*", "--port", "5005"]